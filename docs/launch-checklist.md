# 数感探索乐园 · 上架检查清单

微信小程序正式发布前的自查清单（基于当前代码状态，2026-08）。

## 一、账号与主体（最重要，决定能否上架）

- [ ] **确认主体类型**：后台 → 设置 → 基本设置 → 主体信息
  - 教育类目要求**企业 / 个体工商户**主体；个人主体基本无法选择教育类目
  - 若是个人主体：需注册企业账号并做**微信认证**（企业 300 元/年，个体工商户 30 元/年）
- [ ] 完成**微信认证**（未认证无法提审教育/工具类目，也无法开通支付等能力）
- [ ] AppID 已切换：`wxcd90aaa7911c23ae`（`project.config.json`，已完成 ✓）
- [ ] 登录微信号具备该小程序**管理员/开发者**权限（后台 → 成员管理）

## 二、代码状态（已完成 ✓）

- [x] **移除调试入口**：主页连点跳跳解锁全部关卡（`onFrogTap`）—— 已清除
- [x] **移除云开发初始化**：`wx.cloud.init({ traceUser: true })` 已从 `app.js` 移除
  - 原因：AI 互动板块暂缓，且 `traceUser` 会记录用户 OpenID，与"不收集"的隐私声明保持一致
- [x] **纯本地运行**：存档（`ml_progress_v1`）、钱包（`ml_wallet`）全部存手机本地，**零网络请求、零个人信息上传**
- [x] **无域名白名单问题**：小程序不发起任何 `wx.request`
- [x] **全部测试通过：核心/5 大模块/数字钱包/AI 逻辑（322 断言，实测全过）
- [ ] **重新上传代码**：新 AppID 需在开发者工具重新「上传」（旧账号的版本记录不迁移）

> ⚠️ 若以后启用 AI 互动板块：需恢复 `app.js` 的 `wx.cloud.init`、部署 `cloudfunctions/aiChat`，
> 并在后台**用户隐私保护指引**中如实声明（会收集 OpenID 用于 AI 服务），否则审核/上线有合规风险。

## 三、类目 / 名称 / 简介

- **类目建议**：优先「教育 → 教育信息服务」（通常需要办学许可等资质）；资质不全可考虑「工具」类目（需与实际功能匹配，审核以实际功能为准）
- **名称**：「数感探索乐园」—— 提审前在后台确认无重名、不含"最/第一/官方"等违禁词
- **简介**：避免绝对化用语，建议：「小学数感启蒙游戏，5 大模块闯关 + 数字钱包财商启蒙，纯本地无广告」

## 四、隐私保护指引（提审前在后台填写）

- 本项目**不收集任何个人信息**，指引中如实勾选"不收集"即可
- 数字钱包为**虚拟积分**（家务奖励模拟），非真实货币、无支付、无提现 —— 提审说明中注明

## 五、审核注意点

1. **儿童内容自查**：全站内容为数学练习与家务奖励模拟，无不良/诱导内容
2. **"数字钱包"措辞**：审核可能关注"钱包/零钱"字样。提审说明中解释：这是财商启蒙的虚拟奖励系统（完成任务得积分 → 储蓄吃利息 → 模拟消费），**不涉及任何真实资金**
3. **AI 技能分包**（`app.json` 的 `agent.skills`，依赖 `wx.modelContext`）：若审核/上架遇到兼容问题，可在 `app.json` 中移除该配置（主包功能不受影响）
4. **体验版先测**：后台 → 版本管理 → 生成体验版二维码，让家人/朋友在真机完整走一遍（二维码有时效，过期重新生成）

## 六、提审与发布

1. 开发者工具「上传」→ 填版本号（如 `1.0.0`）与描述
2. 后台「版本管理」→ 提交审核 → 填写类目/页面信息/测试说明
3. 审核通过（一般 1~7 天）→ 点「**发布**」→ 全量或灰度
4. 发布后为正式版，**永久可用，不再"过期"**

---

### 提审说明（可直接复制粘贴到后台）

> 本小程序为面向低龄儿童的数学数感启蒙练习工具，包含 5 大闯关模块（数数、凑十、生活情境、数字关系、时间与钱币）与数字钱包财商启蒙功能。所有数据（学习进度、虚拟积分）均存储在用户手机本地，不收集、不上传任何个人信息，无广告、无内购、无外部链接。数字钱包为家务奖励的虚拟积分模拟，用于财商教育，不涉及真实货币交易。
